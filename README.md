## TypeScript-Test-Jest : 

### Jest 
* npx ts-jest config:init 
* spec e test => São convencoes, normalmente spec são testes
unitarios e test são os de integração(a integracao do meu sistema com express )
index.spec.ts ou index.test.ts

`
 * it('Descricao do teste (IT), () => {})'
 * test('Descricao do teste (TEST), () => {})'
 * 
 * Para ter um grupo de testes 
 * describe('Testando varias coisas', ()=> {
 
 * it('Descricao do teste (IT), () => {})'
 * test('Descricao do teste (TEST), () => {})'
 * })
`
TDD - é escrever o teste antes de fazer o código. 
